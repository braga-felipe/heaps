mutation USER_LOGIN($options: UserLoginInput!) {
  userLogin(options: $options) {
    errors {
      field
      message
    }
    user {
      username
      id
      email
      address
      zipCode
    }
  }
}