import { extendTheme } from '@chakra-ui/react';

const customTheme = extendTheme({
  colors: {
    primary: '#5D55B4',
    secondary: '#DFB23F',
  },
  fonts: {},
  fontWeight: {},
});

export default customTheme;
